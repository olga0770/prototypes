# UI tests for Bicycle web app


## To run the tests:

$ ./node_modules/.bin/cypress run --spec cypress/integration/test-connectivity.js

or in a UI:

$ $(npm bin)/cypress open

and then select test-connectivity.js

